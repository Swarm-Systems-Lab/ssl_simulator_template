rules: []

# Generic secret-detection rules now live in ssl_pydev's own bundled
# baseline, applied automatically by `ssl-pydev security` / `just security`.
# This file only carries project-specific config (excludes/rules) layered
# on top of that baseline plus the p/ci registry pack.
# (no excludes - tests/ is the only Python code in this repo)
